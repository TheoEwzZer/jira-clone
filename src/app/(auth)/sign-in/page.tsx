"use client";

import { SignInCard } from "@/features/auth/components/sign-in-card";
import { ReactElement } from "react";

export const SignInPage: () => ReactElement = (): ReactElement => {
  return <SignInCard />;
};

export default SignInPage;
