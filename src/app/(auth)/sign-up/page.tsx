"use client";

import { SignUpCard } from "@/features/auth/components/sign-up-card";
import { ReactElement } from "react";

export const SignUpPage: () => ReactElement = (): ReactElement => {
  return <SignUpCard />;
};

export default SignUpPage;
